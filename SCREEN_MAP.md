# 遊戲介面地圖 v0.6.0

目前不依賴立繪也可以先把遊戲的層級搭好。每個畫面負責一種心情與一種操作，不把所有按鍵塞在同一頁。

```text
開場動畫（intro overlay）
        ↓ 點擊解鎖
主選單／基地入口
  ├─ 開始邂逅 → 伴學路線 → 角色選擇 → 學習／劇情
  ├─ 角色養成 → 角色列表 → 角色詳情 → 升級／好感度／技能
  ├─ 日語學習 → 課程列表 → 題型選擇 → 結算獎勵
  ├─ 出擊冒險 → 隊伍編成 → 關卡地圖 → 戰鬥 → 戰利品
  └─ 我的基地 → 角色休息 → 回憶 → 裝飾（後續開放）
```

v0.6.0 先完成主選單層，角色養成與日語學習入口會先共用既有角色選擇頁；出擊與基地先以鎖定狀態保留位置，等資料與戰鬥 MVP 完成後解鎖。這樣之後新增功能不需要重做首頁。

## 建議的畫面檔案

- `game.html`：遊戲殼與畫面容器
- `game.js`：畫面切換、共用狀態與事件
- `data/characters.js`：角色固定資料
- `data/content.js`：課程、敵人與關卡資料
- 下一階段：`screens/roster.js`、`screens/lesson.js`、`screens/stage.js`、`screens/battle.js`
